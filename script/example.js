var engine = require("../engine").engine;

var intake = engine.intake.create();
var exhaust = engine.exhaust.create();
var cylinder1 = engine.cylinder.create();
var cylinder2 = engine.cylinder.create();
